package com.apps.network

/**
 * Configuration constants for remote network services.
 */
object NetworkConfig {
    /** Base URL string for remote HTTP API endpoints. */
    const val BASE_URL = "https://static.leboncoin.fr/"
}